#!/usr/bin/env fish

set -l tmuxsessions (tmux list-sessions)

set -l choice (tmux list-sessions | fzf | awk -F ':' '{print $1}')

if test -z $TMUX
    tmux attach -t "$choice"
else
    tmux switch-client -t "$choice"
end
