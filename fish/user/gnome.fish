function gnome-disable-animations
    gsettings set org.gnome.desktop.interface enable-animations false
end

function gnome-enable-animations
    gsettings set org.gnome.desktop.interface enable-animations true
end
