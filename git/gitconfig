[credential "https://github.com"]
	helper = 
	helper = !gh auth git-credential
[credential "https://gist.github.com"]
	helper = 
	helper = !gh auth git-credential
[user]
	name = Nadir Fejzic
	email = nadir@notfloor.com
	signingkey = 0D70B2CFAE6C70DE

[core]
	pager = delta
	excludesFile = ~/Developer/config/git/ignore

[interactive]
	diffFilter = delta --color-only

[delta]
	navigate = true
	side-by-size = true

    # delta detects terminal colors automatically; set one of these to disable auto-detection
    # dark = true
    # light = true

[merge]
	conflictStyle = zdiff3

[diff]
    colorMoved = default
[commit]
	gpgsign = false
